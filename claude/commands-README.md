# Claude Code Custom Commands

Note: Claude Code uses built-in subagent system.
For project-specific instructions, use CLAUDE.md files.

Available at project root:
- CLAUDE.md (read automatically by Claude Code)
- .cursorrules (alternative format)

Global configuration:
- ~/.claude/CLAUDE.md (this directory)
- ~/.claude/mcp_settings.json (MCP servers)
